issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - funlen
    - path: /
      linters:
        - gci
        - gofumpt
        - exhaustivestruct
        - wrapcheck
        - tagliatelle
        - dupl
  enable-all: true
